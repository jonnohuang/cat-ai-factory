# Demo Analyses Canon (Planner-Only)

This directory stores planner-side `video_analysis.v1` metadata artifacts and the
`video_analysis_index.v1` lookup index.

Rules:
- Metadata only; do not store copyrighted source media files here.
- Worker runtime must not depend on these artifacts.
- Keep paths stable so deterministic query results can reference `relpath`.
