# Cat AI Factory — Dev Requirements
#
# This file is for LOCAL development tooling and lightweight runtime deps.
# It intentionally avoids heavy frameworks and cloud SDK lock-in.
#
# Install:
#   pip install -r repo/requirements-dev.txt
# Reproducible analyzer stack (recommended for quality pipeline):
#   pip install -r repo/requirements-dev.txt -c repo/requirements-analyzer-lock.txt

# --- Core validation ---
jsonschema>=4.21.0

# --- Local env / secrets (runtime injected; never committed) ---
python-dotenv>=1.0.1

# --- HTTP client (planner providers / adapters) ---
requests>=2.31.0
python-telegram-bot>=21.0.1
google-auth>=2.35.0

# --- Dev/test tooling ---
pytest>=8.0.0
ruff>=0.3.0
black>=24.1.0

# --- Optional: LangGraph demo (planner-only) ---
langgraph>=0.1.0

# --- Optional: CrewAI demo (planner-only) ---
crewai>=0.35.0

# --- Optional: deterministic CV tooling (planner/offline + worker utilities) ---
opencv-python-headless>=4.10.0
scenedetect>=0.6.3
librosa>=0.10.2
# MediaPipe may not ship wheels for all Python versions/platforms.
mediapipe>=0.10.14; python_version < "3.12"

# --- Optional: static typing (keep lightweight) ---
mypy>=1.8.0
